# Full content of app.py should be obtained here and only the last line modified accordingly. Assuming the user wants the existing content as is.

# Original app.py contents...


if __name__ == '__main__':
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 5000)), debug=False)